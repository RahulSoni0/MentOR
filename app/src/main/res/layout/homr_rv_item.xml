<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="25dp"
    android:clickable="true"
    android:focusable="true"
    android:layout_margin="4dp"
    android:foreground="?android:attr/selectableItemBackground"
    android:elevation="3dp"
    android:translationZ="5dp"
    android:background="@drawable/rounded_corner_bacgkround_white"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/home_rv_itemIcon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/alarm"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/home_rv_itemTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cengage Physics"
        android:layout_marginTop="12dp"
        android:textSize="16sp"
        android:textColor="#4FC3F7"
        app:layout_constraintEnd_toEndOf="@+id/home_rv_itemIcon"
        app:layout_constraintStart_toStartOf="@+id/home_rv_itemIcon"
        app:layout_constraintTop_toBottomOf="@+id/home_rv_itemIcon"
        tools:ignore="HardcodedText" />

    <TextView
        android:id="@+id/home_rv_itemCaption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BN Sharma"
        android:layout_marginTop="8dp"
        android:textColor="#67D478"
        app:layout_constraintEnd_toEndOf="@+id/home_rv_itemTitle"
        app:layout_constraintStart_toStartOf="@+id/home_rv_itemTitle"
        app:layout_constraintTop_toBottomOf="@+id/home_rv_itemTitle"
        tools:ignore="HardcodedText" />

</androidx.constraintlayout.widget.ConstraintLayout>